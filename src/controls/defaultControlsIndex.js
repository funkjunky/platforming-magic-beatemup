import xbox1 from './mappings/xbox1';

// this is default, to be changed by the player on an input changing screen.
const mapping = {
  jump: xbox1.a,
  fireball: xbox1.y,
  swing: xbox1.x,
  dash: xbox1.b,
  pause: xbox1.start,
};

export default mapping;
